services:
  canary:
    build: .
    volumes:
      - .:/app
    working_dir: /app
    environment:
      - CANARY_DATA_DIR=/app/data
    entrypoint: ["sh", "/app/docker-entrypoint.sh"]
    command: ["pytest", "-q"]
