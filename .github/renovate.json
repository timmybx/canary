{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "docker:pinDigests"
  ],
  "timezone": "America/Denver",
  "pip-compile": {
    "managerFilePatterns": ["/(^|/)requirements(-dev|-build|-ci)?\\.txt$/"],
    "rangeStrategy": "bump"
  },
  "pip_requirements": {
    "enabled": false
  },
  "poetry": {
    "enabled": false
  },
  "pre-commit": {
    "enabled": true,
    "pinDigests": true
  },
  "packageRules": [
    {
      "matchDatasources": ["docker"],
      "matchPackageNames": ["python"],
      "allowedVersions": "<3.13",
      "automerge": false
    },
    {
      "matchManagers": ["github-actions"],
      "matchDepNames": ["python"],
      "allowedVersions": "<3.13"
    },
    {
      "description": "Keep requires-python in pyproject.toml on the 3.12 container",
      "matchManagers": ["pip-compile"],
      "matchDepNames": ["python"],
      "allowedVersions": "<3.13"
    },
    {
      "description": "Throttle fuzzer Docker digest updates to weekly",
      "matchManagers": ["dockerfile"],
      "matchFileNames": [".clusterfuzzlite/Dockerfile"],
      "schedule": ["on monday"]
    }
  ]
}
